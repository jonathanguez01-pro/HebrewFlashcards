import SwiftUI

/// Citizen Café–inspired palette from the Design Bible, via the asset catalog.
enum Brand {
    static let charcoal = Color("Charcoal")
    static let accent = Color("AccentColor") // #F5C518
    static let canvas = Color("WarmOffWhite")
}
